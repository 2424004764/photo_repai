[project]
name = "photo-repair"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=6.0.0",
    "imagecodecs>=2026.6.6",
    "opencv-python-headless>=4.13.0.92",
    "pillow>=12.2.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.21.0",
]
